{{#if hasBlock}}
  {{yield
    (hash
      isLoading=fetchData.isRunning
      data=customField
    )
  }}
{{else}}
  {{#if fetchData.isRunning}}
    {{t "customField.loading"}}
  {{else}}
    {{customField.value}}
  {{/if}}
{{/if}}
