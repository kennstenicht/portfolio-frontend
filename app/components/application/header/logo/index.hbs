<div
  block:scope
  role="button"
  {{did-insert this.setElement}}
  {{on "mouseenter" this.explode}}
  ...attributes
>
  {{#each this.defaultLetters as |letter|}}
    <span block:class="letter" data-selector="letter">
      {{letter}}
    </span>
  {{/each}}
</div>
