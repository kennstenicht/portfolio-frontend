<div block:scope ...attributes>
  <AnimatedValue
    @value={{@project.slug}}
    @use={{this.backgroundTransition}}
    @duration={{this.duration}}
    as |slug|
  >
    <div block:class="background" block:style={{slug}}></div>
  </AnimatedValue>

  <header block:class="header">
    <AnimatedValue
      @value={{@project.title}}
      @use={{this.typoTransition}}
      @duration={{this.duration}}
      as |title|
    >
      <h1 block:class="title">
        {{title}}
      </h1>
    </AnimatedValue>

    <AnimatedValue
      @value={{@project.subtitle}}
      @use={{this.typoTransition}}
      @duration={{this.duration}}
      as |subtitle|
    >
      <div block:class="subtitle">
        {{subtitle}}
      </div>
    </AnimatedValue>
  </header>
</div>
