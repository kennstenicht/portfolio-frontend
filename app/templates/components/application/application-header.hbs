<div class="{{elem 'logo'}}">Christoph Wiedenmann</div>
<div class="{{elem 'navigation'}}">
  <ul>
    <li>{{link-to 'Projects' 'projects'}}</li>
    {{#if session.isAuthenticated}}
      <li class='o-list-inline__item'>{{link-to 'Admin' 'admin.projects'}}</li>
      <li class='o-list-inline__item'>{{session/sign-out signedOut=(route-action 'transitionTo' 'home')}}</li>
    {{/if}}
  </ul>
</div>
